model:
  backend: "pi05"
  name: "lerobot/pi05_base"
  action_dim: 7
  image_size: 224
  chunk_size: 50
  chunk_stride: 10

lora:
  target_modules: []

training:
  learning_rate: 1.0e-5
  gradient_checkpointing: false
